FROM python:3.11-slim

# Pinned for reproducible production builds and Python 3.11 compatibility.
# Update these versions intentionally after validating the production stack.
RUN pip install --no-cache-dir \
    mlflow==2.17.2 \
    psycopg2-binary==2.9.10

EXPOSE 5000
